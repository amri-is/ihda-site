function Services() {
  return (
    <section
      id="services"
      className="h-screen flex flex-col justify-center px-8 max-w-3xl mx-auto w-full relative"
    >

      <div className="text-xs uppercase tracking-[.25em] text-rose">
        What we do
      </div>

      <h1 className="font-serif text-5xl max-w-3xl">
        Four ways to be&nbsp;
        <span className="font-curvy text-[3.5rem]">
          stylized
        </span>
      </h1>

      <p className="text-base text-inksoft max-w-md mt-8">
        Every service is built around the occasion, not a fixed formula — the same trained hand,
        <span className="text-ink">&nbsp;your call.</span>
      </p>

    </section>
  )
}

export default Services